{
  "name": "streaming-test-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:api": "npm --prefix api start",
    "start:ui": "npm --prefix ui run dev",
    "dev": "concurrently \"npm run start:api\" \"npm run start:ui\"",
    "seed": "npm --prefix api run seed",
    "seed:reset": "npm --prefix api run seed:reset"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}