import { ResolverDemoResolver } from './resolver-demo.resolver';

describe('ResolverDemoResolver', () => {
  it('should be created', () => {
    const resolver = new ResolverDemoResolver();
    expect(resolver).toBeTruthy();
  });
});
